name: Deploy to K3s Cluster

on:
  push:
    branches:
      - main
      - backend-connect
    paths:
      - "k8s/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:
  workflow_run:
    workflows: ["attendance-service", "scheduling-service", "ai-vision-service"]
    types:
      - completed
    branches:
      - main
      - backend-connect

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'push' || github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Kubeconfig
        run: |
          mkdir -p ~/.kube
          printf '%s' "${{ secrets.KUBECONFIG1 }}" > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Replace DockerHub Username in Kubernetes Manifests
        run: |
          sed -i "s|DOCKERHUB_USERNAME_PLACEHOLDER|${{ secrets.DOCKERHUB_USERNAME }}|g" k8s/*.yaml

      - name: Deploy to K3s Cluster
        run: |
          kubectl apply -f k8s/namespace.yaml
          kubectl apply -f k8s/configmap.yaml
          kubectl apply -f k8s/scheduling-service.yaml
          kubectl apply -f k8s/attendance-service.yaml
          kubectl apply -f k8s/ai-vision-service.yaml
          kubectl apply -f k8s/traefik-middleware.yaml
          kubectl apply -f k8s/ingress.yaml

      - name: Rollout Restart Deployments
        run: |
          kubectl rollout restart deployment/scheduling-service -n smart-attendance
          kubectl rollout restart deployment/attendance-service -n smart-attendance
          kubectl rollout restart deployment/ai-vision-service -n smart-attendance
